# ~/.tmuxinator/tek.yml

project_name: Quiz
project_root: ~/workspace/quiz
socket_name: quiz
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim -c NERDTree
        - git status
        - tmux resize-pan -R 100 
  - logs: 
      layout: even-vertical
      panes: 
        - #tail -f /var/log/apache2/tek.dev.sapo.pt_phperror.log
