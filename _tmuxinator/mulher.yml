# ~/.tmuxinator/comunidades.yml

project_name: Sapo Mulher
project_root: ~/workspace/sapomulher
socket_name: sapomulher
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim -c NERDTree
        - tmux resize-pan -R 100 && git status
  #- logs: 
      #layout: even-vertical
      #panes: 
        #- tail -f /var/log/comunidades.log
        #- tail -f /var/log/comunidades_libsapo_php.log
