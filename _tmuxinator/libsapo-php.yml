# ~/.tmuxinator/comunidades.yml

project_name: LibSapoPHP
project_root: ~/workspace/libsapo-php
socket_name: libsapo-php
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim -c NERDTree
        - git status
        - tmux resize-pan -R 100 && phpunit  
  #- logs: 
      #layout: even-vertical
      #panes: 
        #- tail -f /var/log/comunidades.log
        #- tail -f /var/log/comunidades_libsapo_php.log
