#!/usr/bin/env bash
# bash_profile is called only upon login (at least in linux)

#case $- in *i*) . ~/.bashrc;; esac

#from now on, I'll always include .bashrc, and decide between interactive/non-interactive configuration there
. ~/.bashrc
